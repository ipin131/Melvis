// This file runs before every spec file.
// Import custom commands and global configuration here.
import './commands';

// Silence uncaught exceptions from the app so tests don't fail on unrelated errors
// Cypress.on('uncaught:exception', () => false);
